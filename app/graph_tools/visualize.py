# A good approach is hybrid:
# Use t-SNE or UMAP to get 2D positions for nodes.
# Draw a graph on top (edges for top-k neighbors or retrieved documents).
# Color nodes by relevance, cluster, or query.
